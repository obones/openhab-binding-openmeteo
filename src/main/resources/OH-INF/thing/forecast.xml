<?xml version="1.0" encoding="UTF-8"?>
<!-- OpenMeteo Binding -->
<thing:thing-descriptions bindingId="openmeteo"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:thing="https://openhab.org/schemas/thing-description/v1.0.0"
	xsi:schemaLocation="https://openhab.org/schemas/thing-description/v1.0.0 https://openhab.org/schemas/thing-description-1.0.0.xsd">

	<!-- -->
	<!-- OpenMeteo forecast thing -->
	<!-- -->
	<!--

		We don't use the XML definition because it is very tedious to create the numerous split channel group
		definitions as there is no withChannelGroup on the ThingBuilder returned by editThing().
		Instead, we use a ThingTypeProvider that creates the appropriate elements programatically.

	-->
</thing:thing-descriptions>
